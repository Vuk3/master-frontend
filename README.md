# Object Detection Dashboard

React dashboard for comparing object detection responses from the gateway, Python API, and .NET API.

## Setup

```bash
npm install
```

Create `.env` from `.env.example` if you need a custom gateway URL.

## Run

```bash
npm run dev
```

## Build

```bash
npm run build
```
